//calulating th area in length and width of a plot of land
function Area(Width,length){
    return width*length
}
let width=3;
let length=2;
let total=Area(width,length);
console.log(`the area is${total}`)